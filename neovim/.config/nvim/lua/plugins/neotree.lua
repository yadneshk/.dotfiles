-- ~/.config/nvim/lua/plugins/neotree.lua
return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    window = {
      width = 30, -- Set Neo-tree width (adjust as needed)
      statusline = false, -- Ensure statusline still shows (from previous setup)
    },
  },
}
